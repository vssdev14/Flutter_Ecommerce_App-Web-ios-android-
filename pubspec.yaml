name: assignment
description: A new Flutter project.

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  auto_route: ^7.0.0  # Use latest stable version
  bloc: ^7.0.0
  cupertino_icons: ^1.0.2
  flutter_bloc: ^7.0.0
  flutter_screenutil: ^5.0.0
  http: ^0.13.1
  injectable: ^2.0.0
  lottie: ^1.0.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  auto_route_generator: ^7.0.0
  build_runner: ^2.0.0
  injectable_generator: ^2.0.0
  freezed: ^0.14.1+2

flutter:

  uses-material-design: true
  
  assets:
   
    - assets/lottiefile/
